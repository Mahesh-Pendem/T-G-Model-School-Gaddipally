# MongoDB connection (required for API)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/tgms
# Optional: database name (default: tgms)
MONGODB_DB=tgms
# JWT secret for auth (change in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
